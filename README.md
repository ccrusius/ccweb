CCWEB
=====

My own literate programming system, language agnostic, and written in Lua.

The CMake-based build system can generate native binaries for you, otherwise
you can use the Lua files committed in the `bin` directory, which I check
in for repository usage convenience.

For documentation, see the [generated PDF file](doc/ccweb.pdf).
